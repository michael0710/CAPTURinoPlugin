# TODO check if calling the plugin returns its purpose and version
if (CMAKE_C_COMPILER_ID STREQUAL "MSVC")
  add_test(NAME TestTest
    COMMAND ${CMAKE_CURRENT_BINARY_DIR}/../CapturinoPlugin.exe
  )
elseif (CMAKE_C_COMPILER_ID STREQUAL "GNU")
  add_test(NAME TestTest
    COMMAND ${CMAKE_CURRENT_BINARY_DIR}/../CapturinoPlugin
  )
else()
    message(STATUS "CMAKE_C_COMPILER_ID is ${CMAKE_C_COMPILER_ID}")
    message(FATAL_ERROR "Unknown compiler for this project")
endif()